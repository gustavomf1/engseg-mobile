class AppConfig {
  static const apiBaseUrl = 'http://engseg-api:8080';
  static const bffBaseUrl = 'http://engseg-mobile-backend:8081';
  static const connectTimeout = Duration(seconds: 30);
  static const receiveTimeout = Duration(seconds: 60);
}
